class_name EnemyJumper extends EnemyController

# Will jump around to random offsets.
# When the player is visible in front of it, it will jump towards the player
#	on whichever axis is furthest away.


func telegraph(player: PlayerController, world: WorldInterface) -> void:
	pass


func update() -> void:
	pass
